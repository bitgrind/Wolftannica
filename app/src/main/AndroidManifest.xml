<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kstedman.mathapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Wolftannica"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SolveActivity" />
        <activity android:name=".ui.ConvertActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.ContactActivity" />
        <activity android:name=".ui.ResponseDetailActivity" />
        <activity android:name=".ui.SavedResponseListActivity" />
        <activity android:name=".ui.CreateAccountActivity"></activity>
    </application>

</manifest>